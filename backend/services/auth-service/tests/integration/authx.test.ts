import { describe, it, expect } from 'vitest';

describe.skip('AuthX Integration Tests', () => {
  it('Tests need to be rewritten for Drizzle ORM/PostgreSQL', () => {
    expect(true).toBe(true);
  });
});
